services:
  - type: web
    name: fta-editor
    env: docker
    region: oregon
    plan: free
    branch: main
    dockerfilePath: ./Dockerfile.render
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 5000
      - key: SECRET_KEY
        generateValue: true
